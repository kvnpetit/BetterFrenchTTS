# Module better-french-tts

## Historical API documentation

This reference is regenerated from the original release tag using the current
documentation toolchain. It describes that release's source declarations, not
the latest API. Source links point to the original tag. Regeneration does not
change the library artifact or the release.

Use the version selector to switch releases. APIs and package names can differ
between versions; when a page did not exist in the selected release, use its
documentation home page.

Find the matching release and its tagged README and integration guide through
[GitHub releases](https://github.com/kvnpetit/better-french-tts/releases).
The 1.x releases use the old package names.
